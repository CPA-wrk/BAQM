% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lm_plot.lev.R
\name{lm_plot.lev}
\alias{lm_plot.lev}
\title{Plot Standard Residuals vs. Leverage with Cook's Distance Contours}
\usage{
lm_plot.lev(
  mdl,
  opt = list(),
  parm = list(),
  df = lm_plot.df(mdl),
  plts = list()
)
}
\arguments{
\item{mdl}{An object of class \code{lm} or similar, representing the fitted model.}

\item{df}{Data frame with augmented model data. Defaults to \code{lm_plot.df(mdl)}.}

\item{plts}{List of ggplot objects to build upon. Defaults to empty list.}

\item{parms}{List of plot customization parameters. Passed to \code{lm_plot.parms()}.}
}
\value{
A list containing:
  \item{mdl}{The model object}
  \item{parms}{Parameters used for plotting}
  \item{df}{Data frame used for plotting}
  \item{plts}{A list of ggplot objects, including the leverage plot}
}
\description{
Creates a plot of standard residuals versus leverage values, including Cook's distance contours to visualize influential observations.
}
\details{
The plot displays standard residuals against leverage, overlays Cook's distance contours, and marks outliers based on residuals and Cook's distance. Outlier and influential points can be labeled, and a loess fit line is optionally added.
}
\examples{
\dontrun{
mdl <- lm(Sepal.Length ~ Sepal.Width, data = iris)
result <- lm_plot.lev(mdl)
print(result$plts$lev)
}
}
\seealso{
\code{\link{lm_plot.df}}, \code{\link{lm_plot.parms}}
}
