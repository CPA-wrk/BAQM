#' @keywords internal
"_PACKAGE"
# The following prevents R CMD check notes about global variables
# used in non-standard evaluation (e.g. in data.table expressions).
utils::globalVariables(c(".attn", ".fits", ".hat", ".id", ".lower.pi",
                         ".obs", ".quantile", ".resid", ".sequence",
                         ".std.resid", ".stud.resid", ".upper.pi", "x"))
#
## usethis namespace: start
## usethis namespace: end
NULL
